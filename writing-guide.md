# Allreva 문서 작성 기준

Allreva 문서는 사람이 먼저 읽는다. Agent도 문서를 참고하지만, Agent를 위해 사람이 읽기 어려운 문서를 만들지는 않는다.

## 기본 원칙

- 한국어를 기본으로 쓴다. 코드, API, 명령어, 클래스명처럼 식별이 필요한 이름은 원문 영어를 쓴다.
- 첫 문단에서 이 문서가 답하려는 질문이나 결론을 말한다.
- 사실, 아직 확인하지 못한 가설, 확정된 결정을 구분한다.
- 수치나 성능 개선을 적을 때는 측정 조건과 근거를 함께 적는다.
- 처음 보는 사람이 모를 수 있는 용어는 처음 한 번만 짧게 설명한다.
- 한 문서가 길어지면 목차를 늘리기보다 주제별 문서로 나눈 뒤 연결한다.

## 피할 표현

근거 없이 아래 같은 표현을 쓰지 않는다.

- 효율적인, 견고한, 최적의, 획기적인
- 완벽하게, 확실히, 반드시
- 다음과 같습니다, 이를 통해, 성공적으로

필요하면 주장 대신 관찰한 사실을 쓴다.

```text
나쁨: 캐시를 적용해 성능을 획기적으로 개선했다.
좋음: 동일한 조건의 조회 테스트에서 평균 응답 시간이 150ms에서 5ms로 줄었다.
```

## 문장과 구성

- 한 문장에는 주장 하나를 둔다.
- 결론을 먼저 쓰고, 뒤에 이유와 근거를 둔다.
- 문서와 PR에서 가운데점(`·`)을 구분 기호로 쓰지 않는다. 쉼표, `및`, 콜론, 문장 분리를 사용한다.
- 목록은 실제로 비교하거나 확인할 항목이 있을 때만 쓴다.
- 표는 항목을 빠르게 비교할 때만 쓴다. 문단 전체를 표로 바꾸지 않는다.
- 제목은 내용을 설명한다. `개요`, `정리`, `기타`처럼 넓은 제목은 가능한 피한다.

## ASCII 다이어그램

- 구조나 흐름을 짧게 보여 줄 때는 Markdown code block 안에 ASCII 다이어그램을 쓴다.
- 상자, 선, 화살표, 라벨은 영문과 ASCII 문자만 사용한다. 한글이나 Unicode 선 문자는 정렬이 깨질 수 있다.
- 다이어그램 밖의 설명은 한국어로 쓴다.
- 다이어그램은 한 화면에서 읽을 수 있을 정도로 유지한다. 세부 흐름은 문서나 Mermaid 다이어그램으로 나눈다.

## 문서별 최소 정보

개발 workflow에서는 새 RFC를 만들거나 Issue, plan, PR에 연결하지 않는다. 기존 RFC는 당시 검토 기록으로만 보존한다.

- ADR: 여러 작업에 오래 영향을 주는 중요한 아키텍처, 운영, 정책 결정을 확정했을 때 무엇을 결정했는지, 왜 이 선택을 했는지, 감수한 점을 적는다.
- 문제 해결 기록: 증상, 원인, 해결, 검증 결과를 적는다.

## 작성 후 확인

- 이 문서의 결론을 첫 화면에서 찾을 수 있는가?
- 주장이 측정값, 코드, Issue, PR 중 하나로 뒷받침되는가?
- 처음 보는 팀원이 모르는 약어와 전제가 남아 있지 않은가?
- 지금 코드와 다를 수 있는 부분을 현재 기준처럼 쓰지 않았는가?
- 문장을 소리 내어 읽었을 때 불필요하게 딱딱하거나 과장되지 않는가?
