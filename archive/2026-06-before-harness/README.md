# 하네스 정비 전 자료

이 폴더는 문서 하네스를 만들기 전에 로컬에 있던 자료를 보존한 곳이다. 아직 Git에 올라가지 않았던 아키텍처 설명, API 문서, 문제 해결 기록, 이미지와 부하 테스트 파일이 들어 있다.

이 자료는 삭제하지 않고 보존한다. 다만 현재 코드와의 일치 여부를 모두 확인하지 않았으므로, 현재 설계 규칙이나 구현 근거로 바로 사용하지 않는다.

새 문서를 만들 때 필요한 사실과 측정 결과는 원본 코드·실험 결과와 함께 다시 확인한 뒤, 알맞은 현재 문서 또는 `evidence/`로 옮긴다.
