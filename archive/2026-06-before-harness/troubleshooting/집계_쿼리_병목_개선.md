---
created_at: 2026-04-30T15:05:00
updated_at:
---
# 개요

현재 ALLREVA 프로젝트에서 차량 대절 신청 시 발생하는 문제와 해결 방안을 정리한다.

1. 데이터 증가에 따른 rent_participants 조회 성능 저하

soft delete를 적용하면 rent_participants(신청자) 데이터가 계속 누적된다.

boarding_date를 기준으로 WHERE 조건을 걸어 조회하지만, 데이터가 많아질수록 성능이 저하된다. boarding_date에 인덱스를 추가하면 성능을 개선할 수 있으나, 다음과 같은 한계가 있다.

- COUNT 쿼리로 인해 설계 복잡도 증가
- JOIN 과정에서 rent_participants 테이블 스캔이 사실상 필수
- 단순 COUNT가 아니라 SUM(passenger_num)을 계산해야 하므로 쿼리가 더 복잡해짐

요구사항이 변경될 때마다 쿼리가 더 복잡해져 도메인 이해도와 유지보수성이 떨어진다.

예를 들어 rent_participants를 JOIN한 뒤 SUB QUERY로 SUM을 구하는 방식은 가독성이 낮고 성능도 좋지 않다.

### 기존 QueryDSL 쿼리 예시

```java
public List<RentParticipantResponse> findRentParticipant(final Long memberId) {
    QRent rentSub = new QRent("rentSub");
    QRentBoardingSlot rentBoardingSlotSub = new QRentBoardingSlot("rentBoardingSlotSub");
    QRentParticipant rentParticipantSub = new QRentParticipant("rentParticipantSub");

    return queryFactory.select(Projections.constructor(RentParticipantResponse.class,
            rent.id,
            rent.detailInfo.title,
            rentBoardingSlot.date,
            rent.operationInfo.boardingArea,
            rent.createdAt,
            rent.additionalInfo.endDate,
            rent.additionalInfo.recruitmentCount,
            ExpressionUtils.as(
                    JPAExpressions.select(rentParticipantSub.passengerNum.sum())
                            .from(rentSub)
                            .join(rentBoardingSlotSub).on(rentSub.id.eq(rentBoardingSlotSub.rent.id))
                            .join(rentParticipantSub).on(rentBoardingSlotSub.date.eq(rentParticipantSub.boardingDate))
                            .where(rentSub.id.eq(rent.id)),
                    "passengerNum"
            ),
            rent.isClosed,
            rentParticipant.id,
            rentParticipant.createdAt,
            rentParticipant.passengerNum,
            rentParticipant.boardingType,
            rentParticipant.depositor.depositorName,
            rentParticipant.depositor.depositorTime,
            rentParticipant.refundType
    ))
            .from(rent)
            .join(rentBoardingSlot).on(rent.id.eq(rentBoardingSlot.rent.id))
            .join(rentParticipant).on(rentBoardingSlot.date.eq(rentParticipant.boardingDate))
            .where(rentParticipant.memberId.eq(memberId))
            .groupBy(rent.id, rentBoardingSlot.date, rentParticipant.id)
            .fetch();
}
```

이 방식은 복잡한 GROUP BY와 SUB QUERY를 유발해 SQL 실행 계획이 비효율적으로 동작하게 만든다.

### 기존 Aggregation Query를 활용한 조회 성능 테스트

테스트 환경:

- CPU: 13th Gen Intel(R) Core(TM) i5-1340P
- RAM: 16GB
- MySQL: 8.0.39
- 데이터 개수:
    - rent: 10,000개
    - rent_boarding_slot: 30,000개
    - rent_participants: 1,020,000개
- 테스트 기준: [rent.id](http://rent.id/) = 500
- 실행 계획: EXPLAIN ANALYZE

Aggregation Query 성능 측정:

```sql
select
    r1_0.id,
    rbs1_0.date,
    r1_0.recruitment_count,
    sum(rp1_0.passenger_num)
from
    rents r1_0
    left join rent_boarding_slot rbs1_0
        on r1_0.id = rbs1_0.rent_id
    left join rent_participants rp1_0
        on rp1_0.rent_id = r1_0.id
        and rbs1_0.date = rp1_0.boarding_date
where
    r1_0.id = ?
    and rbs1_0.date = ?
    and r1_0.member_id = ?
group by
    r1_0.id,
    rbs1_0.date;
```

결과:

- 실행 계획:
    
    ```
    -> Group aggregate: sum(rp1_0.passenger_num) (cost=403603 rows=86285) (actual time=668..668 rows=1 loops=1)
        -> Left hash join (no condition) (cost=373098 rows=305051) (actual time=668..668 rows=34 loops=1)
    				-> Filter: (rbs1_0.`date` = DATE'2025-09-11') (cost=0.78 rows=0.3) (actual time=0.0504..0.055 rows=1 loops=1)
    						-> Index lookup on rbs1_0 using FKg0pi9e6l1ch0r5slo7er7hotd (rent_id=500) (cost=0.78 rows=3) (actual time=0.0475..0.051 rows=3 loops=1)
    				-> Hash
    						-> Filter: ((rp1_0.boarding_date = DATE'2025-09-11') and (rp1_0.rent_id = 500)) (cost=341751 rows=1.02e+6) (actual time=23.3..668 rows=34 loops=1)
    								-> Table scan on rp1_0 (cost=341751 rows=1.02e+6) (actual time=0.106..519 rows=1.02e+6 loops=1)
    ```
    
- 평균 실행 시간: 0.5s ~ 0.7s
    
- 인덱스 추가: CREATE INDEX idx_rent_participants_boarding_date ON rent_participants (boarding_date);
    
- 실행 계획:
    
    ```
    -> Group aggregate: sum(rp1_0.passenger_num) (cost=340 rows=389) (actual time=14.5..14.5 rows=1 loops=1)
    	  -> Nested loop left join (cost=265 rows=755) (actual time=1.08..14.5 rows=34 loops=1)
    				-> Filter: (rbs1_0.`date` = DATE'2025-09-11') (cost=0.78 rows=0.3) (actual time=0.0469..0.0538 rows=1 loops=1)
    						-> Index lookup on rbs1_0 using FKg0pi9e6l1ch0r5slo7er7hotd (rent_id=500) (cost=0.78 rows=3) (actual time=0.0444..0.0499 rows=3 loops=1)
    				-> Filter: ((rp1_0.boarding_date = DATE'2025-09-11') and (rp1_0.rent_id = 500)) (cost=1468 rows=2516) (actual time=1.03..14.4 rows=34 loops=1)
    						-> Index lookup on rp1_0 using idx_rent_participants_boarding_date (boarding_date='2025-09-11') (cost=1468 rows=2516) (actual time=0.231..14.1 rows=2516 loops=1)
    ```
    
- 인덱스 추가로 성능이 개선됨.
    

💡 GROUP BY는 정렬에 영향을 주지만, 인덱스가 있으면 괜찮은 성능을 보인다.

### SUB QUERY 성능 측정:

```sql
select
		r1_0.id,
		rbs1_0.date,
		(select
				 sum(rp2_0.passenger_num)
		from
				rent r3_0
		join rent_boarding_slot rbs2_0
				on r3_0.id=rbs2_0.rent_id
		join rent_participants rp2_0
				on rbs2_0.date=rp2_0.boarding_date
		where
				r3_0.id=r1_0.id),
		rp1_0.id,
		rp1_0.passenger_num
from
		rent r1_0
join rent_boarding_slot rbs1_0
		on r1_0.id=rbs1_0.rent_id
join rent_participants rp1_0
		on rbs1_0.date=rp1_0.boarding_date
where
		rp1_0.member_id=?
group by
		r1_0.id,
		rbs1_0.date,
		rp1_0.id;
```

결과:

- 여러 인덱스를 추가해도 1분 이상 조회 불가
- rent_id마다 SUB QUERY가 추가로 발생 → 매우 비효율적
- 쿼리 작성 난이도 자체가 급격히 증가

### 대안: 신청자 수 컬럼 역정규화 적용

보다 효율적인 조회를 위해 rent_boarding_slot 테이블에 passenger_count 컬럼을 추가하여 역정규화를 적용한다.

차량 대절을 신청할 때 rent_participants 테이블에 레코드를 추가하기 전, passenger_count 값을 증가시키는 방식.

기존 방식 vs. 개선 방식

- 기존: SUM(passenger_num)을 rent_participants에서 계산
- 개선: passenger_count 컬럼에서 직접 조회

쿼리 복잡도가 줄고 단순 조회로 성능이 크게 향상된다.

### 역정규화 적용 후 테스트

테스트 환경:

위와 동일

Aggregation Query 성능 측정:

```sql
select
		r1_0.id,
		rbs1_0.date,
		rbs1_0.recruitment_count,
		rbs1_0.passenger_count
from
		rent r1_0
left join rent_boarding_slot rbs1_0
		on r1_0.id=rbs1_0.rent_id
where
		r1_0.id=? and
		rbs1_0.date=? and
		r1_0.member_id=?;
```

결과:

- 실행 계획:
    
    ```
    -> Filter: (rbs1_0.date = DATE'2025-09-11') (cost=0.78 rows=0.3) (actual time=0.0265..0.0307 rows=1 loops=1)
    		-> Index lookup on rbs1_0 using FKg0pi9e6l1ch0r5slo7er7hotd (rent_id=500) (cost=0.78 rows=3) (actual time=0.0242..0.0274 rows=3 loops=1)
    ```
    
- 인덱스 제거한 상태에서 0.0307s
    

Postgresql로 실험

```
allreva=> EXPLAIN ANALYZE
SELECT r.id, rbs.date, rbs.recruitment_count, rbs.passenger_count
FROM rent AS r
LEFT JOIN rent_boarding_slots AS rbs ON r.id = rbs.rent_id
WHERE r.id = 1 AND rbs.date = '2025-09-11' AND r.member_id = 1;
                                                                            QUERY PLAN
------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Nested Loop  (cost=0.57..16.62 rows=1 width=20) (actual time=0.051..0.054 rows=1 loops=1)
   ->  Index Scan using rent_pkey on rent r  (cost=0.29..8.30 rows=1 width=8) (actual time=0.034..0.034 rows=1 loops=1)
         Index Cond: (id = 1)
         Filter: (member_id = 1)
   ->  Index Scan using idx_rent_boarding_slots_rent_date on rent_boarding_slots rbs  (cost=0.29..8.31 rows=1 width=20) (actual time=0.015..0.016 rows=1 loops=1)
         Index Cond: ((rent_id = 1) AND (date = '2025-09-11'::date))
 Planning Time: 0.215 ms
 Execution Time: 0.094 ms
(8 rows)
```

0.074 ~ 0.125ms

### SUB QUERY 성능 측정:

passenger_count 추가로 SUB QUERY 필요 없음.

```sql
select
		r1_0.id,
		rbs1_0.date,
		rbs1_0.recruitment_count,
		rbs1_0.passenger_count,
		rp1_0.id,
		rp1_0.passenger_num
from
		rent r1_0
join rent_boarding_slot rbs1_0
		on r1_0.id=rbs1_0.rent_id
join rent_participants rp1_0
		on rbs1_0.date=rp1_0.boarding_date
where
		rp1_0.member_id=?;
```

결과:

- 실행 계획:

```
-> Inner hash join (rp1_0.boarding_date = rbs1_0.date) (cost=30.4e+6 rows=3e+6) (actual time=398..1331 rows=18175 loops=1)
		-> Filter: (rp1_0.member_id = 500) (cost=13 rows=10168) (actual time=9.91..694 rows=216 loops=1)
				-> Table scan on rp1_0 (cost=13 rows=1.02e+6) (actual time=0.106..583 rows=1.02e+6 loops=1)
		-> Hash
				-> Nested loop inner join (cost=11311 rows=29496) (actual time=0.164..129 rows=30000 loops=1)
						-> Covering index scan on r1_0 using PRIMARY (cost=987 rows=9799) (actual time=0.0753..5.97 rows=10000 loops=1)
						-> Index lookup on rbs1_0 using FKg0pi9e6l1ch0r5slo7er7hotd (rent_id=r1_0.id) (cost=0.753 rows=3.01) (actual time=0.00975..0.0117 rows=3 loops=10000)
```

- rent_participants 풀 스캔 등 병목 존재, 1.892s
- 결과가 나온 것 자체가 의미 있음

이거는 쿼리 나눠서 실행해보자. 해볼게.

### 추가 효과:

기존 검증 로직:

```java
private void checkPassengersMaximumReached(
				final Long rentId,
				final LocalDate boardingDate,
				final Integer passengerNum
) {
		Rent rent = rentRepository.findById(rentId)
						.orElseThrow(RentNotFoundException::new); // 1. rent 조회 쿼리
		int maximumCount = rent.getAdditionalInfo().getRecruitmentCount();
		
		Integer currentPassengerCount = rentParticipantRepository.countRentParticipants(rentId, boardingDate); // 2. 집계 함수 쿼리
		if (currentPassengerCount + passengerNum > maximumCount) {
		    throw new PassengersMaximumReachedException();
		}
		// rent close event
		if (currentPassengerCount + passengerNum == maximumCount) {
		    Events.raise(new RentClosedEvent(rentId));
		}
}
```

- rent에서 모집 인원 조회 후 rent_participants에서 Aggregation으로 신청 인원 계산 → 복잡

개선 로직:

```java
private void checkPassengersMaximumReached(
				final Long rentId,
				final LocalDate boardingDate,
				final Integer passengerNum
) {
		RentBoardingSlot rentBoardingSlot = rentRepository.findRentBoardingSlot(rentId, boardingDate)
		.orElseThrow(() -> new RentNotFoundException(rentId));
		int maximumCount = rentBoardingSlot.getRecruitmentCount();
		int currentPassengerCount = rentBoardingSlot.getPassengerCount();
		
		if (currentPassengerCount + passengerNum > maximumCount) {
		    throw new PassengersMaximumReachedException();
		}
}
```

- 역정규화로 조회 성능 향상
- 비즈니스 로직 집중
- 🤔 동시성 이슈는 별도 해결 필요

이외에도 QueryDSL을 이용해 쿼리 코드를 작성하는 과정에서도 복잡성을 대폭 줄일 수 있었다.

### 결론

passenger_count 컬럼을 rent_boarding_slot 테이블에 추가해 역정규화하면 쿼리가 단순해지고 성능이 크게 개선된다.

개선 효과

- Aggregation → 인덱스 추가 시 성능 차이 미미
- SUB QUERY 제거 → 조회 성능 향상
- QueryDSL 쿼리 단순화 → 유지보수성 ↑

1. 느낀 점

- Aggregation Query는 신중히 도입
- 데이터 모델링 시 count 데이터는 단일 컬럼으로 관리 고려
- 동시성 등 부수적 문제는 추가 공부 필요

### 차량 대여 참여 상세 조회 구하기

```
allreva=> EXPLAIN ANALYZE
SELECT rp.boarding_date, rp.rent_id, rp.member_id, rp.id, rp.passenger_num
FROM rent_participants AS rp
WHERE rp.member_id = 1
ORDER BY rp.boarding_date DESC
LIMIT 10;
                                                                      QUERY PLAN
-------------------------------------------------------------------------------------------------------------------------------------------------------
 Limit  (cost=26203.44..26204.61 rows=10 width=32) (actual time=53.960..55.398 rows=10 loops=1)
   ->  Gather Merge  (cost=26203.44..29172.12 rows=25444 width=32) (actual time=53.958..55.396 rows=10 loops=1)
         Workers Planned: 2
         Workers Launched: 2
         ->  Sort  (cost=25203.42..25235.22 rows=12722 width=32) (actual time=47.127..47.128 rows=10 loops=3)
               Sort Key: boarding_date DESC
               Sort Method: top-N heapsort  Memory: 26kB
               Worker 0:  Sort Method: top-N heapsort  Memory: 26kB
               Worker 1:  Sort Method: top-N heapsort  Memory: 26kB
               ->  Parallel Seq Scan on rent_participants rp  (cost=0.00..24928.50 rows=12722 width=32) (actual time=0.019..45.866 rows=10000 loops=3)
                     Filter: (member_id = 1)
                     Rows Removed by Filter: 330000
 Planning Time: 0.162 ms
 Execution Time: 55.462 ms
(14 rows)
```

```
allreva=> EXPLAIN ANALYZE
SELECT rp.boarding_date, rp.rent_id, rp.member_id, rp.id, rp.passenger_num
FROM rent_participants AS rp
WHERE rp.member_id = 1
ORDER BY rp.boarding_date DESC
LIMIT 10;
                                                                          QUERY PLAN                                                                   
--------------------------------------------------------------------------------------------------------------------------------------------------------------
 Limit  (cost=21874.87..21874.89 rows=10 width=32) (actual time=66.749..66.750 rows=10 loops=1)
   ->  Sort  (cost=21874.87..21951.20 rows=30532 width=32) (actual time=66.747..66.748 rows=10 loops=1)
         Sort Key: boarding_date DESC
         Sort Method: top-N heapsort  Memory: 26kB
         ->  Bitmap Heap Scan on rent_participants rp  (cost=345.05..21215.08 rows=30532 width=32) (actual time=5.820..64.017 rows=30000 loops=1)
               Recheck Cond: (member_id = 1)
               Heap Blocks: exact=19615
               ->  Bitmap Index Scan on idx_rent_participants_member_id  (cost=0.00..337.42 rows=30532 width=0) (actual time=2.782..2.782 rows=30000 loops=1)
                     Index Cond: (member_id = 1)
 Planning Time: 0.139 ms
 Execution Time: 66.789 ms
(11 rows)
```

idx (member_id, boarding_date DESC) 적용

```
 Limit  (cost=0.42..23.12 rows=10 width=32) (actual time=0.052..0.124 rows=10 loops=1)
   ->  Index Scan using idx_rent_participants on rent_participants rp  (cost=0.42..69294.66 rows=30532 width=32) (actual time=0.050..0.121 rows=10 loops=1)
         Index Cond: (member_id = 1)
 Planning Time: 0.630 ms
 Execution Time: 0.142 ms
(5 rows)
```

idx (member_id) 적용 (default는 ASC임)

```
 Limit  (cost=21874.87..21874.89 rows=10 width=32) (actual time=67.185..67.187 rows=10 loops=1)
   ->  Sort  (cost=21874.87..21951.20 rows=30532 width=32) (actual time=67.184..67.185 rows=10 loops=1)
         Sort Key: boarding_date DESC
         Sort Method: top-N heapsort  Memory: 26kB
         ->  Bitmap Heap Scan on rent_participants rp  (cost=345.05..21215.08 rows=30532 width=32) (actual time=7.509..64.477 rows=30000 loops=1)
               Recheck Cond: (member_id = 1)
               Heap Blocks: exact=19615
               ->  Bitmap Index Scan on idx_rent_participants  (cost=0.00..337.42 rows=30532 width=0) (actual time=2.969..2.969 rows=30000 loops=1)
                     Index Cond: (member_id = 1)
 Planning Time: 0.135 ms
 Execution Time: 67.221 ms
(11 rows)
```

1단계 - Bitmap Index Scan

member_id = 1에 해당하는 row들의 물리적 위치(페이지 번호)를 비트맵으로 수집.

페이지 번호 : 0 1 2 3 4 5 6 7 … 비트맵: 0 1 0 0 1 0 1 0 …

비트맵: bit를 페이지 번호에 대응시킨 배열.

페이지 안에 여러 튜플이 저장되어 있어 같은 페이지를 접근해야 하는 상황 발생. 이럴 때 랜덤 I/O 낭비를 줄이기 위해 비트맵으로 수집한 후 Bitmap Heap Scan 시 비트맵을 보고 실제 Heap 테이블을 읽음.

즉 스캔이 한 번 전체적으로 이루어진 후 힙 영역을 순차로 또 스캔함(이때는 실제 페이지만 순차로 읽음)

즉 member_id=1 데이터들이 물리적으로 많이 분산되어 있다는 소리…

heap sort는 최소 힙 구조임. 결국 30,000개를 전체를 순회해서 가장 우선순위가 최소인 값을 계속 탈락시키는 구조임. 그러면 자연스럽게 우선순위가 높은 10개만 남게되는 구조. 이게 O(n log 10)이기 때문에 정말 쌈. 힙 재구성 비용이 log 10이라서 사실상 상수 수준. 이 힙 재구성을 30,000번 반복하면 되는데 이게 정말 쌈.

문제는 비트맵 스캔임. 인덱스가 있어도 결과 값이 30,000개면 얘기가 달라짐. 랜덤 I/O를 30,000번 수행해야 하기 때문에 성능 제약이 생김. 아무리 Bitmap으로 랜덤 I/O를 줄이려 노력해도 무시할 수 없는 수준임.

idx (member_id, boarding_date) 적용

```
 Limit  (cost=0.42..23.12 rows=10 width=32) (actual time=0.046..0.070 rows=10 loops=1)
   ->  Index Scan Backward using idx_rent_participants on rent_participants rp  (cost=0.42..69294.66 rows=30532 width=32) (actual time=0.044..0.067 rows=10 loops=1)
         Index Cond: (member_id = 1)
 Planning Time: 1.746 ms
 Execution Time: 0.092 ms
(5 rows)
```

아예 member_id에 boarding_date를 포함시켜서 인덱스를 만들면 됨. 정렬이 이미 되어있기 때문에 그냥 순차 조회하면 됨.

Backward로 스캔한건 DESC로 인해서 생긴 이슈.

인덱스를 추가하면 삽입 성능 저하되지만 실제로 단건 삽입만 이루어지기 때문에 거의 없음.